from fastapi import FastAPI
import os

app = FastAPI()

@app.get("/")
def home():
    return {"status": "ok", "service": "kalshi-monitor-api"}

@app.get("/balance")
def balance():
    # aqui luego conectas tu DB o Kalshi real
    return {"balance": 0, "status": "running"}

# Render necesita que corra en 10000
# Start Command debe ser: uvicorn api:app --host 0.0.0.0 --port 10000
